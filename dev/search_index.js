var documenterSearchIndex = {"docs":
[{"category":"section","location":"porting/#Porting-a-randomized-method","page":"Porting a randomized method","text":"Randomized range finders, subspace iterations, RSVD and the trace estimators all do the same four things in a loop: apply the operator to a block of vectors, orthonormalize, repeat, and finish with a small dense factorization. Each has a Funicular counterpart, and porting one is mostly a matter of deciding which matrices become a PanelMatrix and which stay a Matrix.","title":"Porting a randomized method"},{"category":"section","location":"porting/#The-translation-table","page":"Porting a randomized method","text":"dense code Funicular traversal\nΩ = randn(T, N, k) GhostPanels(T, N, k; plan, seed) none, regenerated\nY = G * X panelmul!(Y, G, X) one column-panel sweep\nQ, R = qr(Y) R = cholqr2!(Y), Y becomes Q four row-block sweeps\nB = X' * Y gram(X, Y) one row-block sweep\nV = Q * C rightmul!(V, Q, C) one row-block sweep\nY = Y * C rightmul!(Y, C) one row-block sweep\nS = Q' * G * Q project(Q, G) p (p + 1) panel uploads\nY .+= α .* X axpy!(α, X, Y) one sweep\nY .*= α scale!(Y, α) one sweep\ndot(X, Y), norm(X) the same names one sweep\nQ[:, 1:s] = seed, hcat(Y, Z) copycols!(Q, 1:s, seed) none, a host copy\nB .= A copyto!(B, A) none, a host copy\nZ = similar(Y), Matrix{T}(undef, m, k) similar(Y), similar(Y, T, (m, k)) none, an allocation\nanything else foreachpanel(f!, pm) one sweep\n\nEverything k × k stays an ordinary Matrix on the host: gram and project return one, cholqr2! returns one, and the eigen- or SVD-solve at the end of a randomized method is a plain LAPACK call.","title":"The translation table"},{"category":"section","location":"porting/#Rules-of-thumb","page":"Porting a randomized method","text":"Count sweeps rather than flops. Once the matrix no longer fits on the device, the cost of an operation is how many times it moves the matrix past the GPU. panelmul! is one pass, gram is one, rightmul! is one, cholqr2! is four, and project is p + 1 of them for p panels. A loop body that was free in the dense version because it was a broadcast over a resident array becomes a full sweep here, so fold it into the panel function of a sweep that is happening anyway.\n\nPrefer panelmul! then gram over project. The nested version exists for the case where you cannot afford a second N × k matrix. If you can afford it, panelmul!(Z, G, Q) followed by gram(Q, Z) is two sweeps instead of p + 1.\n\nOrthonormalize between every application. CholeskyQR2 squares the condition number before factoring, so it wants κ(Y) ≲ 1/sqrt(eps), about 1e8 in double precision. Power iteration squares κ each time. Past that limit, cholqr2! falls back to a shifted factorization and a third pass, which recovers orthogonality but costs more than staying below it would have.\n\nDo not reach for indexing. There is no getindex. When you want a column, use either a sweep or, for a matrix that fits in host memory, Matrix(pm), and which of those it is depends on whether the answer is k × k or N × k.\n\nWhat two matrices have to share depends on how the operation traverses them. A column-panel operation takes panel j of both, so both have to be cut into the same panels: same k and same w, with the row counts free. That is panelmul!, axpy! and everything else that acts a column at a time. A row-block operation takes rows r of both, so both have to have the same N, with the column counts and the widths free. That is gram and rightmul!. Either way the two belong to one plan, since the plan owns the staging buffers the operation runs through.\n\nThe width is what to watch when the two matrices have different row counts. A plan left to choose the width takes N into account as well as k, so an m × k matrix and an n × k one built from the same plan can come out cut differently and then have no panel in common, which panelmul! refuses. Build the companion as similar(X, T, (m, k)), which carries X's width across the change of row count, or fix panel_width on the plan and take the same width everywhere.","title":"Rules of thumb"},{"category":"section","location":"porting/#A-worked-shape","page":"Porting a randomized method","text":"The randomized range finder, as the demo writes it:\n\nfunction rangefinder(G, Ω, plan; iterations = 2)\n    Y = PanelMatrix{eltype(Ω)}(undef, size(Ω)...; plan = plan)\n    Z = PanelMatrix{eltype(Ω)}(undef, size(Ω)...; plan = plan)\n    panelmul!(Y, G, Ω)\n    cholqr2!(Y)\n    for _ in 1:iterations\n        panelmul!(Z, G, Y)\n        cholqr2!(Z)\n        Y, Z = Z, Y\n    end\n    Y, Z\nend\n\nThe two panel matrices are allocated once and swapped rather than reallocated each iteration, because allocating a panel matrix means page-locked host memory and, under a scratch_dir, a file, and neither is cheap enough to do in a loop.\n\nThe trace estimators port the same way. Their random probes become GhostPanels, their A * Ω becomes panelmul!, and their traces become gram or dot.","title":"A worked shape"},{"category":"section","location":"porting/#What-has-to-stay-outside","page":"Porting a randomized method","text":"Funicular provides storage, movement and panel BLAS. The methods themselves stay where they are. cholqr2! is the single exception, and it lives here because its second pass is a sweep and it cannot be written efficiently from outside the executor.","title":"What has to stay outside"},{"category":"section","location":"residency/#Residency","page":"Residency","text":"","title":"Residency"},{"category":"section","location":"residency/#The-three-tiers","page":"Residency","text":"A panel is on exactly one tier at a time.\n\nThe device tier is where the work happens. Panels are never resident there: they are staged through buffers the ResidencyPlan allocated once and reuses for every sweep. Nothing inside a sweep loop allocates device memory.\n\nThe host tier is page-locked memory the plan owns as slabs, with one block per panel viewing into a slab. The copies stay asynchronous because the memory is page-locked. Slabs are allocated on demand and double in size, so a host_budget of hundreds of gigabytes does not cost minutes at startup.\n\nThe disk tier is one HDF5 file per matrix, with one dataset chunked at exactly one panel, so a read or a write always covers a whole chunk rather than a strided slice of several. It exists when the plan has a scratch_dir and HDF5.jl is loaded. Panels go there when the host tier runs out of room, and come back on the sweep's producer task a step or two ahead of the pipeline, so the read overlaps the transfers rather than stalling them.\n\nA fourth kind of panel has no storage at all: GhostPanels regenerates its columns from a seed and the column index whenever a panel is asked for, and drops them rather than writing them when host memory runs short. That is the right representation for the random test matrix of a randomized method. One seed means one matrix whatever the panel width, so two machines that can afford different panel sizes still generate the same test matrix.","title":"The three tiers"},{"category":"section","location":"residency/#Panel-width","page":"Residency","text":"w is uniform across a matrix, with a ragged last panel when it does not divide k. Left alone, ResidencyPlan picks the widest panel whose buffers fit in device_budget, capped at target_panel_bytes (2 GiB by default), and then evens the width out over the panels it implies: a k = 1000 matrix whose budget allows w = 999 is cut into two panels of 500 rather than one of 999 and one of 1.\n\nOverride it with panel_width on the plan or w on one matrix. Two matrices that meet in a column-panel operation have to be cut the same way, and the automatic choice depends on N, so a companion of a different height comes out cut differently unless it is built with similar. Porting a randomized method has the rule for each traversal. copyto! between two matrices of different widths cuts a matrix into other panels.\n\nWider panels mean fewer, larger transfers and better use of the bus. They also mean more device memory per staging buffer and a coarser unit of work for the pipeline to hide. Somewhere between one and four gigabytes per panel is the range this was designed around.","title":"Panel width"},{"category":"section","location":"residency/#Sizing-the-budgets","page":"Residency","text":"device_budget has to cover, at once:\n\nnbuffers staging buffers per panel matrix taking part in a sweep, each N × w in the compute eltype,\na second set of those in the storage eltype when the host tier is narrowed,\nworkspace_bytes(G), the operator's own device memory,\nthe small k × k matrices an operation keeps on the device: an accumulator for gram and project, the uploaded factor for rightmul!,\none buffer beyond the sweep's own for project, which is a panel, and for the in-place rightmul!, which is a row block.\n\nTake it from the device's total memory minus a reserve rather than from CUDA.free_memory(), which under-reports once the memory pool holds cached blocks. Ten percent held back is a reasonable reserve. When the budget runs out the error names the buffer that did not fit, what is already allocated, and the cap, so read the message rather than guessing.\n\nhost_budget caps the page-locked tier. With a scratch_dir it no longer has to hold the whole matrix, but it does have to hold what a sweep touches at once:\n\na panel sweep (panelmul!, axpy!, scale!, foreachpanel) stages up to four steps ahead of the pipeline, one panel per matrix per step, so four panels per matrix in the sweep;\na row sweep (gram, cholqr2!, rightmul!) works on blocks of rows that span every panel, so it holds every panel of its matrix in host memory at once.\n\nThat second one is the real floor: host_budget cannot go below one whole matrix while an operation traverses rows. A sweep that cannot get host memory raises an error saying so rather than silently degrading.","title":"Sizing the budgets"},{"category":"section","location":"residency/#Precision","page":"Residency","text":"PanelMatrix{T} carries the compute eltype, that is, what device kernels and the operator see. The cold tiers can be narrower. host_eltype = ComplexF32 under a ComplexF64 compute eltype halves both the host memory and the bytes crossing the bus, and the conversion is a device kernel fused into the transfer rather than a separate pass. disk_eltype must equal host_eltype, since a panel moves between those two tiers unconverted.\n\nNarrowing is opt-in and never happens by default. Gram matrices, R factors and projections are computed in the compute eltype whatever the storage does.","title":"Precision"},{"category":"section","location":"residency/#Moving-panels-by-hand","page":"Residency","text":"Sweeps handle residency themselves. Outside a sweep:\n\nFunicular.prefetch!(pm, j) brings panel j to the host tier.\nFunicular.evict!(pm.panels[j], plan) sends it back to its cold tier, writing it out first if it changed. A panel with no colder tier, and no ghost home to regenerate from, has nowhere to go, and the call says so.\nFunicular.free!(pm) gives every block back and closes the matrix's scratch file. A process that exits without it leaves the file behind.\nsave writes a matrix to a named file and load opens one with its panels still cold.","title":"Moving panels by hand"},{"category":"section","location":"operators/#The-operator-contract","page":"Operators","text":"panelmul! and project take any G that implements four operations, plus three more that are optional. There is no abstract type to subtype and nothing to register. The methods listed below are the whole contract.","title":"The operator contract"},{"category":"section","location":"operators/#What-is-required","page":"Operators","text":"Base.size(G)                       # (rows, cols), both positive\nBase.size(G, d)\nBase.eltype(G)                     # a concrete number type\nBase.adjoint(G)                    # an object satisfying this same contract\nLinearAlgebra.mul!(y, G, x)        # y and x are device vectors\n\nmul! is handed device-resident arrays: CuArray views under CUDA, MtlArray views under Metal, plain Array views on the reference backend. Write it against AbstractVector and let the arrays decide, the way the operators in test/operators.jl do.\n\nFunicular applies an operator to whole columns, so an m × n G reads columns of an n-row matrix and writes columns of an m-row one. panelmul! takes any such shape. project is the exception and needs a square G, since Q' G Q makes sense only if G maps a space into itself.","title":"What is required"},{"category":"section","location":"operators/#What-is-optional","page":"Operators","text":"Of the three, workspace_bytes is the one worth implementing carefully. Funicular's budget arithmetic holds that many bytes back from the device pool before allocating any staging buffer, so an operator that under-reports it turns an honest error at plan construction into an out-of-memory failure part way through a run. This matters for an operator holding an FFT plan with a large work area, or any other factor it keeps resident between applications.\n\npanel_capable says mul!(Y, G, X) takes a matrix. When it is false, which is the default, panelmul! loops the columns of the resident panel itself. That costs nothing extra in transfers and only gives up whatever batching the operator might have done.","title":"What is optional"},{"category":"section","location":"operators/#Checking-it","page":"Operators","text":"Run it once, when you write an operator, against the backend the operator will be applied on. It probes shape, eltype, adjoint consistency ⟨y, Gx⟩ = ⟨G'y, x⟩, and both claims when they are made. It allocates several probe blocks outside any plan's pool, so it is a diagnostic rather than something to call in a loop.","title":"Checking it"},{"category":"section","location":"operators/#LinearMaps.jl","page":"Operators","text":"Once LinearMaps.jl is loaded, a LinearMap satisfies the contract with no further work, with panel_capable true and ishermitian_op reading the map's own trait. Note that LinearMaps declines to inspect a matrix it wraps, so LinearMap(A) is Hermitian only if you say LinearMap(A; ishermitian = true).\n\nGoing the other way:","title":"LinearMaps.jl"},{"category":"section","location":"operators/#An-example","page":"Operators","text":"The demo defines a Hermitian, panel-capable, matrix-free operator with an honest workspace_bytes in about twenty lines.","title":"An example"},{"category":"function","location":"operators/#Funicular.workspace_bytes","page":"Operators","text":"Funicular.workspace_bytes(G) -> Int\n\nReturns the device memory G needs for itself while it is applied, for instance the work area of an FFT plan. ResidencyPlan holds that much back from the buffer pool, so an operator that under-reports it overflows the device. Defaults to 0.\n\n\n\n\n\n","title":"Funicular.workspace_bytes"},{"category":"function","location":"operators/#Funicular.panel_capable","page":"Operators","text":"Funicular.panel_capable(G) -> Bool\n\nWhether mul!(Y, G, X) accepts whole panels, that is matrices of several columns at once, rather than one vector at a time. Defaults to false, which makes panelmul! loop over the columns of the resident panel.\n\n\n\n\n\n","title":"Funicular.panel_capable"},{"category":"function","location":"operators/#Funicular.ishermitian_op","page":"Operators","text":"Funicular.ishermitian_op(G) -> Bool\n\nWhether G is its own adjoint. Defaults to false, which is always safe. Funicular does not yet take a shortcut on the strength of it. It is declared so that callers and check_operator can ask, and so that an operator that answers wrongly is caught before something starts trusting it.\n\n\n\n\n\n","title":"Funicular.ishermitian_op"},{"category":"function","location":"operators/#Funicular.check_operator","page":"Operators","text":"check_operator(G; n=3, backend=CPUBackend(), rtol=nothing) -> true\n\nHolds G to the contract panelmul! and project expect, using n random probe vectors, and throws an ArgumentError naming the first thing that fails. Checks that size, eltype and adjoint answer sensibly, and that G and its adjoint agree: ⟨y, Gx⟩ = ⟨G'y, x⟩. An operator claiming Funicular.panel_capable is additionally checked to give the same answer for a whole panel as it does column by column, and one claiming Funicular.ishermitian_op to satisfy ⟨y, Gx⟩ = ⟨Gy, x⟩.\n\nA rectangular G is checked the same way. For an m × n operator the probes x have n rows and the probes y have m, so each side of ⟨y, Gx⟩ = ⟨G'y, x⟩ is an inner product in the space it belongs to, and an adjoint that maps the wrong way round fails the shape check before the probes run. Only the Hermitian claim needs a square operator, and a rectangular operator that claims it is refused.\n\nThe probes are allocated outside any plan's pool, four blocks of n columns and two more when the panel and Hermitian claims are made, so a check on a full size operator costs several vectors of device memory.\n\nArguments\n\nn::Integer=3: The number of probe vectors\nbackend::DeviceBackend=CPUBackend(): Where the probes are allocated. Must be the backend the operator expects to be applied on\nrtol=nothing: Relative tolerance for the probe comparisons, defaulting to a multiple of eps scaled by the operator size\n\n\n\n\n\n","title":"Funicular.check_operator"},{"category":"function","location":"operators/#Funicular.linearmap","page":"Operators","text":"Funicular.linearmap(G) -> LinearMap\n\nWraps an operator that satisfies the Funicular contract as a LinearMaps.LinearMap, so code written against LinearMaps.jl can consume it. The map applies G and its adjoint, carries G's size and eltype, and declares Hermitian symmetry when Funicular.ishermitian_op does.\n\nThe other direction needs nothing: a LinearMap already satisfies the contract, and loading LinearMaps.jl tells Funicular that one takes whole panels and when one is Hermitian. Neither crossing carries Funicular.workspace_bytes, since a LinearMap has no way to hold it, so pass the operator's workspace to ResidencyPlan yourself.\n\nNeeds LinearMaps.jl loaded.\n\n\n\n\n\n","title":"Funicular.linearmap"},{"category":"section","location":"when-to-use/#When-to-use-this","page":"When to use this","text":"Funicular is built for one specific situation, and this page is meant to help you decide whether you are in it. For most tall-skinny work you are not, and something simpler will serve you better.","title":"When to use this"},{"category":"section","location":"when-to-use/#Use-it-if","page":"When to use this","text":"The matrix is tall and skinny, and the column is the meaningful unit. That is, millions of rows and at most a few thousand columns, applied to and combined a column at a time, with no single entry ever wanted on its own. Randomized range finders, subspace iterations, block Krylov bases and trace estimators all produce matrices of this shape.\nA panel fits on the device but the matrix does not. One column, and in practice a block of a few dozen, has room in device memory next to the operator and its workspace, while the full basis does not and may not fit in host memory either. This is the case Funicular is designed for.\nApplying the operator costs more than moving a column. Call the time to apply your operator to one column C, and the time to move that column to the device and back T. Funicular hides the transfers behind the compute, so it helps when C is comfortably larger than T, and helps more the larger the ratio is. The arithmetic is given below.\nYou touch the columns in sweeps. A streaming pipeline can schedule ahead of itself when every column is touched in order, over and over. An access pattern that jumps around cannot be prefetched and gets nothing from any of this.","title":"Use it if"},{"category":"section","location":"when-to-use/#Do-not-use-it-if","page":"When to use this","text":"Everything already fits in device memory. A device matrix and the vendor's BLAS do this well, and Funicular can only add transfers to it: a panel matrix keeps its panels in host memory, so a sweep pays an upload and a writeback that a resident matrix does not. The pipeline hides all but the first upload and the last writeback, and benchmark/resident.jl measures what is left. In that case, use raw device arrays.\nA single column does not fit on the device. The operator itself then has to be split across devices, and that belongs to a different layer than this one. That case is out of scope permanently, though nothing here forecloses it: the executor never assumes a device panel is any particular array type, only that the vendor API can move it, so a distributed panel type could be slotted in later.\nYour operator is cheap next to the transfers, so you are bandwidth bound. Double buffering still overlaps the two directions of the link, but there is no compute to hide behind and the run costs what the transfers cost. Measure before you commit: benchmark/overlap.jl sweeps the ratio and reports what the pipeline recovers at each one.\nYou want to index the matrix. There is no getindex and there will not be: a scalar read of a cold panel is a panel-sized disk read for sixteen bytes, and cheap-looking syntax should not hide a cost like that. Use Matrix(pm) when the whole matrix fits in host memory, foreachpanel to get one panel at a time on the device, and the panel BLAS functions for everything else.\nThe data is larger than host memory and disk put together. No storage layer helps with that, so the problem itself has to be made smaller.","title":"Do not use it if"},{"category":"section","location":"when-to-use/#The-arithmetic","page":"When to use this","text":"In the regime above, the transfers cost essentially nothing. This follows from one comparison. Moving a column across the host link runs at a few tens of gigabytes per second when the host memory is page-locked, while device memory is one to two orders of magnitude faster than that. As such, an operator that reads and writes its argument even a handful of times inside device memory has already spent longer than moving that argument would have taken, and one built on transforms, on products against a factor that stays resident, or on any iterated kernel spends far more. That is the common case for the operators this package is for, and it is why the transfers can be made free rather than merely cheaper.\n\nWrite C for the time to apply the operator to one panel and T for the time to move that panel up and back. A schedule that uploads panel j+1 while the operator works on panel j, and writes panel j-1 back at the same time, costs\n\nmax(C, T) per panel, instead of C + T\n\nso at C/T = 5 it recovers 83% of the transfer time and at C/T = 10, 91%. What is left over is the first upload and the last writeback, which have nothing to overlap against. Deeper buffering would hide those too and is not implemented.\n\nbenchmark/overlap.jl measures this directly. It calibrates a synthetic panel function against the measured copy time, sweeps C/T through 0.5, 1, 2, 5 and 10, and holds the two-buffer pipeline to max(C, T) × 1.15. On the hardware it has been run on, at ratios of 5 and 10, the pipeline costs the compute plus about one percent: every transfer but the two at the ends disappears.","title":"The arithmetic"},{"category":"section","location":"when-to-use/#Two-ways-to-lose-it","page":"When to use this","text":"Both are easy to do by accident, and neither one fails a correctness test.\n\nThe first is pageable host memory. An asynchronous copy out of memory that is not page-locked is not asynchronous: the driver stages it through a bounce buffer of its own and the call blocks. Funicular page-locks the host tier by construction, and benchmark/pinned.jl checks that it still does.\n\nThe second is a synchronization inside the loop. One synchronize() per panel, or one hidden inside a stream-ordered allocation, or a vendor library quietly waiting on a buffer that another stream touched, is enough to serialize the pipeline while every test still passes. benchmark/overlap.jl is the only thing that notices, and it did catch exactly that during development.","title":"Two ways to lose it"},{"category":"section","location":"when-to-use/#The-unified-memory-baseline","page":"When to use this","text":"A GPU can already oversubscribe its memory without any package at all: allocate the matrix in unified memory and let the driver fault pages in as they are touched. benchmark/unified.jl runs the same work both ways and expects Funicular to be at least twice as fast, on the grounds that an explicit schedule that knows what the next panel is should beat demand paging that finds out on a fault. If it ever stops being twice as fast, the pipeline has a bug.","title":"The unified memory baseline"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"Everything Funicular exports. The internal names used elsewhere in these pages are reached as Funicular.name and are listed at the bottom.","title":"Reference"},{"category":"section","location":"reference/#Storage","page":"Reference","text":"Base.copyto! and Base.similar also take panel matrices, as methods on the existing generics rather than new names. copyto!(pm, A) fills a matrix from a dense one, and copyto!(dest, src) copies one panel matrix into another of the same N and k, which is how a matrix is cut into different panels. Both are copycols! over every column. similar(pm), similar(pm, T) and similar(pm, T, dims) build an uninitialized matrix from pm's plan, with pm's panel width.","title":"Storage"},{"category":"section","location":"reference/#Sweeps","page":"Reference","text":"","title":"Sweeps"},{"category":"section","location":"reference/#Panel-linear-algebra","page":"Reference","text":"LinearAlgebra.axpy!, dot and norm also take panel matrices. They are methods on the existing generics rather than new names: axpy!(α, X, Y), dot(X, Y) and norm(X), the last being the Frobenius norm and the only p it accepts.","title":"Panel linear algebra"},{"category":"section","location":"reference/#Operators","page":"Reference","text":"check_operator and the three operator traits have a page of their own: see The operator contract.","title":"Operators"},{"category":"section","location":"reference/#Files","page":"Reference","text":"","title":"Files"},{"category":"section","location":"reference/#Backends","page":"Reference","text":"","title":"Backends"},{"category":"section","location":"reference/#Not-exported","page":"Reference","text":"name what it does\nFunicular.workspace_bytes operator trait, device memory it holds\nFunicular.panel_capable operator trait, mul! takes a matrix\nFunicular.ishermitian_op operator trait, G == G'\nFunicular.linearmap wrap an operator as a LinearMap\nFunicular.plan the plan a matrix was built from\nFunicular.prefetch! bring one panel to the host tier\nFunicular.evict! send one panel back to its cold tier\nFunicular.free! release a matrix's blocks and close its file\nFunicular.diskarray read a Funicular file through DiskArrays.jl\nFunicular.disk_reads panels read from a matrix's store\nFunicular.disk_writes panels written to it\nFunicular.randn_column! the default GhostPanels generator","title":"Not exported"},{"category":"type","location":"reference/#Funicular.ResidencyPlan","page":"Reference","text":"ResidencyPlan(; device_budget, host_budget, backend=CPUBackend(), kwargs...)\n\nOwner of all data Funicular allocates: the device buffer pool through which the pipeline stages panels, and the host slabs where cold panels are stored. Budgets are in bytes and are checked before allocating, so a plan that is too small for a given panel width throws an error.\n\nGiving the plan a scratch_dir means host_budget no longer has to cover the whole matrix. It does still have to cover what a sweep holds at once: a panel sweep stages up to 4 steps ahead of the pipeline, one panel per matrix per step, and gram and cholqr2! traverse rows and so hold every panel of their matrix.\n\nArguments\n\nbackend::DeviceBackend=CPUBackend(): The backend the plan allocates through\ndevice_budget::Real: Bytes the plan may allocate on the device. Take it from the device's total memory minus a reserve\nhost_budget::Real: Bytes the plan may allocate for the host tier, page-locked on a real device backend. Slabs are allocated on demand up to this cap, not upfront, since page-locking is slow\nworkspace_bytes::Real=0: Device memory the operator needs for itself, held back from the buffer pool\nnbuffers::Integer=2: Staging buffers per sweep the budget arithmetic assumes, 2 for the double-buffered pipeline and 1 for the serialized oracle\ntarget_panel_bytes::Real=2 * GiB: Panel size aimed for when choosing the panel width, clamped to what the budget allows\npanel_width=nothing: Fixed panel width, overriding the choice above\nhost_eltype=nothing, disk_eltype=nothing: Storage eltype of the cold tiers, e.g. ComplexF32 under a ComplexF64 compute eltype, with the conversion fused into the transfers. Defaults to the compute eltype, since narrowing must be asked for\nscratch_dir=nothing: Directory for the disk tier. A plan that has one gives every PanelMatrix a file of its own and spills panels to it when the host tier fills up. Needs HDF5.jl loaded\n\n\n\n\n\n","title":"Funicular.ResidencyPlan"},{"category":"type","location":"reference/#Funicular.PanelMatrix","page":"Reference","text":"PanelMatrix{T}(undef, N, k; plan, w=nothing)\nPanelMatrix(A::AbstractMatrix; plan, w=nothing)\n\nTall-skinny N × k matrix of compute eltype T, partitioned into column panels of width w with a ragged last panel when w does not divide k. Each panel is resident on one tier at a time and moves between tiers under the plan's control. Panels start out on the host tier.\n\nThis is deliberately not an AbstractMatrix and has no getindex: a scalar read of a cold panel would cost a whole panel of traffic. Collect it with Matrix(pm) when the result fits in host memory, and otherwise work one panel at a time.\n\nA plan with a scratch_dir gives the matrix a file of its own at construction, and panels spill to it whenever the host tier runs out of room. Reload a saved matrix with load instead of constructing one.\n\nArguments\n\nN::Integer: Number of rows\nk::Integer: Number of columns\nA::AbstractMatrix: Dense matrix whose columns are copied into the new matrix panel by panel\nplan::ResidencyPlan: The plan that owns the tiers the panels live on\nw=nothing: Panel width for this matrix, or nothing to take the plan's choice from the device budget (see ResidencyPlan)\n\n\n\n\n\n","title":"Funicular.PanelMatrix"},{"category":"function","location":"reference/#Funicular.GhostPanels","page":"Reference","text":"GhostPanels([f!], T, N, k; plan, seed=0, w=nothing) -> PanelMatrix{T}\n\nAn N × k matrix of compute eltype T whose panels are generated on demand and never stored. Column col is filled by f!(dst, rng, col), where dst is a host array holding one whole column, rng is a Random.Xoshiro seeded from seed and col alone, and col is the column's index in the full matrix. f! defaults to standard normal entries.\n\ndst is host memory and never a device array, and its eltype is the one the host tier stores: under a narrowing host_eltype that is the narrowed type rather than T. It holds all N rows of the column, so a generator that normalizes what it draws needs nothing from the panels around it.\n\nThe result is an ordinary PanelMatrix apart from being read only, so panelmul!(Y, G, Ω), gram(Ω) and foreachpanel(f, Ω; write=false) all work on it. Anything that would write to it, including cholqr2!, raises an error: a written panel would be thrown away the moment it left host memory.\n\nThis is meant for the test matrix of a randomized method. A 10⁷ × 1000 Gaussian takes 160 GB to store and nothing at all when it is regenerated instead. The generation runs on the sweep's producer task, a step or two ahead of the pipeline, so it overlaps the transfers the way a disk read does. Panels stay in host memory while there is room for them and are dropped rather than written when there is not, so a ghost matrix needs no scratch_dir and no host_budget beyond what a sweep holds at once.\n\nA column's stream comes from its index and the seed, so panels regenerate in any order and one seed means one matrix whatever the panel width. Regeneration is exact within one Julia version: both hash and the Xoshiro stream are Julia implementation details. Two runs on different versions that have to agree need the matrix written out once with save and loaded thereafter.\n\nArguments\n\nf!: Generator for one column, called as f!(dst, rng, col)\nN::Integer: Number of rows\nk::Integer: Number of columns\nplan::ResidencyPlan: The plan that owns the tiers the panels live on\nseed=0: Seed the column's random stream is derived from\nw=nothing: Panel width for this matrix, or nothing to take the plan's choice from the device budget (see ResidencyPlan)\n\nΩ = GhostPanels(ComplexF64, N, k; plan = plan, seed = 0x5EED)\npanelmul!(Y, G, Ω)\n\n\n\n\n\n","title":"Funicular.GhostPanels"},{"category":"function","location":"reference/#Funicular.npanels","page":"Reference","text":"npanels(pm) -> Int\n\nNumber of column panels, cld(k, panelwidth(pm)).\n\n\n\n\n\n","title":"Funicular.npanels"},{"category":"function","location":"reference/#Funicular.panelwidth","page":"Reference","text":"panelwidth(pm) -> Int\npanelwidth(pm, j) -> Int\n\nNominal panel width of pm, or the width of panel j. The last panel is narrower than the nominal width when panelwidth(pm) does not divide k.\n\n\n\n\n\n","title":"Funicular.panelwidth"},{"category":"function","location":"reference/#Funicular.panelrange","page":"Reference","text":"panelrange(pm, j) -> UnitRange{Int}\n\nColumns of the full N × k matrix that panel j holds.\n\n\n\n\n\n","title":"Funicular.panelrange"},{"category":"function","location":"reference/#Funicular.copycols!","page":"Reference","text":"copycols!(dest, dcols, src::AbstractMatrix) -> dest\ncopycols!(dest, dcols, src::PanelMatrix, scols) -> dest\n\nCopies whole columns into columns dcols of dest, either from a dense matrix of that shape or from columns scols of another panel matrix. A starting basis goes into the leading columns of a wider matrix this way, and a sketch grows by building the wider matrix and copying the narrower one into the columns it already holds.\n\nThe copy is host side. There is no sweep and nothing crosses to the device: each destination panel is brought to the host tier, the columns are copied between storage arrays, and the panel is marked dirty. A panel matrix source comes up the same way, so a ghost source regenerates and a disk-backed one is read, and neither is written to. Eltypes convert on the way, so a narrowed storage tier on either side and a source that lives on the device both work.\n\nA range that lines up with the panels is no faster than one that does not. Every case is a host copy between storage arrays, and an aligned range only means fewer of them.\n\nThe whole-matrix copyto!(dest, src) is copycols! with both ranges full. It asks the two matrices for the same N and k and nothing more, so copying between two panel widths is how a matrix is cut into different panels.\n\nArguments\n\ndest::PanelMatrix: The matrix written into, which cannot be a ghost\ndcols::AbstractUnitRange: Columns of dest to write, inside 1:dest.k\nsrc: A dense dest.N × length(dcols) matrix, or a panel matrix with dest.N rows\nscols::AbstractUnitRange: Columns of a panel matrix source to read, as many as dcols names\n\n\n\n\n\n","title":"Funicular.copycols!"},{"category":"function","location":"reference/#Funicular.foreachpanel","page":"Reference","text":"foreachpanel(f!, pm; write=true, nbuffers=nothing)\n\nRuns f!(j, panel) on every panel of pm in order, with panel a device resident view of panel j. This is the escape hatch for device work that Funicular does not provide itself: the sweep handles residency and overlap, and f! does the work on the panel.\n\npanel is a view of a staging buffer, panelwidth(pm, j) columns wide, and is valid only for the duration of the call. Keep f! to work it issues on the device. Its results reach the host when foreachpanel returns, not when f! does.\n\nTwo staging buffers hide the copies behind the compute, while one buffer serializes the pipeline and is the correctness oracle. Panels are visited in order under both, so an accumulation over panels sums in the same order either way.\n\nArguments\n\nf!: Called as f!(j, panel) once per panel\npm::PanelMatrix: The panel matrix to sweep\nwrite::Bool=true: Whether to copy each buffer back into panel j and mark the panel dirty. Pass false for a read-only sweep\nnbuffers=nothing: Number of staging buffers, defaulting to the plan's setting\n\n\n\n\n\n","title":"Funicular.foreachpanel"},{"category":"function","location":"reference/#Funicular.gram","page":"Reference","text":"gram(X[, Y]; nbuffers=nothing) -> Matrix\n\nComputes the k × k matrix X' * Y, accumulating on the device one row block at a time and copying the result back to the host once at the end. gram(X) is gram(X, X) and costs one upload instead of two.\n\nThe sweep goes over blocks of rows rather than over column panels, since a column panel of X only meets the matching columns of Y and that is not enough for X' Y. One row block spans every panel, so the whole matrix moves exactly once, but every panel has to be host resident at the same time.\n\nThe sum accumulates in the compute eltype of X. Narrowing storage through ResidencyPlan does not narrow it, since that keyword sets the eltype of the host and disk tiers and leaves the compute eltype alone.\n\nWith X === Y the result is Hermitian only up to rounding, because the two triangles are computed independently rather than one mirrored onto the other. Wrap it in Hermitian if you need the symmetry exactly.\n\nArguments\n\nnbuffers=nothing: Staging buffers per operand, defaulting to the plan's nbuffers\n\n\n\n\n\n","title":"Funicular.gram"},{"category":"function","location":"reference/#Funicular.panelmul!","page":"Reference","text":"panelmul!(Y, G, X; nbuffers=nothing) -> Y\n\nApplies the operator G to every column of X, writing the result into the matching column of Y, in one sweep over column panels. Panels of X stream up to the device and panels of Y stream back down, both overlapped with the operator's work on the panel in between.\n\nG is m × n, X is n × k and Y is m × k. The operator can be rectangular and the two matrices can have different row counts. They share k, the panel width, and the plan, since a step of the sweep is column panel j of both. G must also satisfy the operator contract: run check_operator on it once. An operator that declares Funicular.panel_capable is handed the whole resident panel, otherwise Y's columns are filled one mul! at a time from the resident panel.\n\nA plan that chooses panel widths for itself takes N into account as well as k, so two matrices of different heights built from one plan can come out cut differently and then have no step in common. When m ≠ n, fix panel_width in the plan, or pass w = panelwidth(X) when building Y.\n\nArguments\n\nnbuffers=nothing: Staging buffers per operand, defaulting to the plan's nbuffers\n\n\n\n\n\n","title":"Funicular.panelmul!"},{"category":"function","location":"reference/#Funicular.rightmul!","page":"Reference","text":"rightmul!(Y, C; nbuffers=nothing) -> Y\nrightmul!(dest, src, C; nbuffers=nothing) -> dest\n\nMultiplies a panel matrix by a small dense factor on the right: Y ← Y C in place, or dest ← src C into a second matrix. In place C is k × k, since a matrix cannot change its own column count. Out of place src is N × s, C is s × r and dest is N × r, so r columns come out of s, which is what truncating a basis needs.\n\nThe sweep goes over blocks of rows rather than over column panels, since a factor on the right mixes columns and a column panel holds only part of each of its rows. One row block spans every panel, so the whole matrix moves exactly once, but every panel has to be host resident at the same time.\n\nThe in-place form needs one device block buffer beyond the sweep's own: a GEMM cannot have its destination alias its source, so each block is multiplied into a temporary and copied back. The out-of-place form needs nothing extra, since src streams up while dest streams down.\n\ndest and src share their rows, their plan and their compute eltype, and are free to differ in column count and panel width. src may be a ghost matrix, so a regenerated test matrix can be multiplied straight into a sketch. dest and the in-place Y are written to and so cannot be ghosts.\n\nC is any AbstractMatrix, UpperTriangular and Adjoint included. It is densified and converted to the compute eltype before it goes to the device, which costs nothing next to the sweep because it is only s × r.\n\nArguments\n\nnbuffers=nothing: Staging buffers per operand, defaulting to the plan's nbuffers\n\n\n\n\n\n","title":"Funicular.rightmul!"},{"category":"function","location":"reference/#Funicular.cholqr2!","page":"Reference","text":"cholqr2!(Y; nbuffers=nothing) -> R\n\nOrthonormalizes the columns of Y in place by CholeskyQR2, returning the k × k upper triangular R with Q * R equal to the original Y, where Q is what Y now holds.\n\nEach of the two passes forms the Gram matrix Y' Y, takes its Cholesky factor on the host, and divides Y by that factor with a triangular solve over row blocks. Two passes cost four sweeps of the whole matrix.\n\nCholeskyQR2 squares the condition number before factoring, so Y must satisfy κ(Y) ≲ eps(real(T))^(-1/2), about 1e8 in double precision, for the Cholesky to succeed outright. Past that the Gram matrix loses positive definiteness and the pass falls back to factoring G + s I with s = 11 N k eps(real(T)) ‖G‖, and a third pass is then needed to recover orthogonality. That is the shifted CholeskyQR3 of Fukaya et al., and it holds up to κ(Y) near 1/eps. Orthonormalizing between power iterations, rather than after several of them, keeps you out of this regime.\n\nAn indefinite Gram matrix raises an error, since the shift cannot rescue it. A merely rank deficient one does survive the shift, and the columns of Y that were dependent come back as columns of Q that are not meaningfully orthonormal. Check norm(gram(Q) - I) if you cannot rule that out.\n\nArguments\n\nnbuffers=nothing: Staging buffers per operand, defaulting to the plan's nbuffers\n\n\n\n\n\n","title":"Funicular.cholqr2!"},{"category":"function","location":"reference/#Funicular.project","page":"Reference","text":"project(Q, G; nbuffers=nothing) -> Matrix\n\nForms the k × k projected operator Q' * G * Q without ever storing G Q.\n\nUnlike panelmul!, this needs a square G matching the row count of Q, since Q' G Q means nothing unless G maps a space into itself.\n\nThis is the one operation here that does not move the matrix a constant number of times. For each panel j it applies G to that panel and then sweeps all of Q against the result to fill one block column of the answer, so it uploads p (p + 1) panels for p = npanels(Q) while applying G only p times. When you can afford another N × k matrix, panelmul!(Z, G, Q) followed by gram(Q, Z) gets the same answer in two sweeps instead of p + 1 of them.\n\nNeeds one device panel buffer beyond the sweep's own, to hold G Q_j while the rest of Q streams past it.\n\nArguments\n\nnbuffers=nothing: Staging buffers per operand, defaulting to the plan's nbuffers\n\n\n\n\n\n","title":"Funicular.project"},{"category":"function","location":"reference/#Funicular.scale!","page":"Reference","text":"scale!(Y, α; nbuffers=nothing) -> Y\n\nMultiplies every entry of Y by α in place, in one sweep.\n\nArguments\n\nnbuffers=nothing: Staging buffers per operand, defaulting to the plan's nbuffers\n\n\n\n\n\n","title":"Funicular.scale!"},{"category":"function","location":"reference/#Funicular.save","page":"Reference","text":"save(pm::PanelMatrix, path) -> path\n\nWrites pm to path as one HDF5 file holding a single chunked dataset with one chunk per panel, in the storage eltype the plan's cold tiers use. The matrix is left where it was: its panels keep whatever residency they had, and the file is closed before this returns.\n\nReload the file with load, or read it from generic chunked-array code with Funicular.diskarray(path).\n\n\n\n\n\n","title":"Funicular.save"},{"category":"function","location":"reference/#Funicular.load","page":"Reference","text":"load(PanelMatrix, path; plan, readonly=false) -> PanelMatrix\nload(PanelMatrix{T}, path; plan, readonly=false) -> PanelMatrix{T}\n\nOpens a file written by save as a PanelMatrix whose panels start out on disk and stream up as they are needed. The compute eltype comes from the file unless the first form is given one. The panel width always comes from the file, since it is the file's chunking.\n\nThe file is the matrix's cold tier rather than a copy of it: a panel that an operation modifies is written back into it. Pass readonly=true to open the file for reading only. Any operation that would modify a panel then raises an error instead of silently editing the file.\n\nThe store is closed by Funicular.free!(pm).\n\n\n\n\n\n","title":"Funicular.load"},{"category":"type","location":"reference/#Funicular.CPUBackend","page":"Reference","text":"CPUBackend(; jitter=0.0)\n\nReference backend where the \"device\" is a plain Array. Queues are chains of tasks instead of streams so that ordering mistakes show up as failures. jitter is the maximum sleep in seconds injected before each queued operation, so a positive value makes the schedule vary between runs and helps test scheduling. Run such tests multiple times with multiple threads to increase the chance of catching race conditions.\n\n\n\n\n\n","title":"Funicular.CPUBackend"},{"category":"function","location":"reference/#Funicular.cuda_backend","page":"Reference","text":"Funicular.cuda_backend()\n\nThe CUDA device backend, available once the CUDA package has been loaded. It is the only backend where the tiers, the streams and the events are real: host slabs are page-locked, copies are asynchronous on explicit streams, and the pipeline overlaps them against compute.\n\n\n\n\n\n","title":"Funicular.cuda_backend"},{"category":"function","location":"reference/#Funicular.metal_backend","page":"Reference","text":"Funicular.metal_backend(; jitter=0.0)\n\nThe Metal device backend, available once the Metal.jl package has been loaded. It exists to run the logic layer against a real GPU array type: it computes in ComplexF32 and Float32 only, and its unified memory makes host/device copies a formality rather than a transfer, so transfers_are_real is false and no timing conclusion follows from it.\n\nArguments\n\njitter=0.0: Maximum sleep in seconds injected before each queued operation\n\n\n\n\n\n","title":"Funicular.metal_backend"},{"category":"section","location":"#Funicular.jl","page":"Home","text":"Tiered-residency storage and a streaming pipeline for tall-skinny complex matrices, plus the panel-granular linear algebra that matrix-free randomized methods need on top of it.\n\nA randomized method applied to a large matrix-free operator keeps a basis of N × k, with N in the tens of millions and k in the hundreds or thousands. At N = 10⁷ one column is 160 MB in ComplexF64, so a thousand of them are 160 GB, past any single GPU and often past the node's RAM as well. One column, or a panel of a few dozen, does fit on the GPU. As such, the matrix is cut into column panels, the panels live on disk or in page-locked host memory, and they stream through the GPU behind the compute that is already running there.\n\nApplying such an operator to a column typically costs several times what moving that column across the host link costs, so with two staging buffers and a schedule that keeps both of them busy the transfers cost essentially nothing. Providing that schedule, and keeping it correct, is the job of this package. When to use this discusses whether your problem is of that kind.","title":"Funicular.jl"},{"category":"section","location":"#Installing-it","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/PaulVirally/Funicular.jl\")\n\nFunicular itself depends only on the standard library and KernelAbstractions.jl. Everything else arrives with a package you load:\n\nload this and you get\nCUDA.jl the real device backend: pinned host memory, streams, the pipeline\nMetal.jl an Apple GPU backend for correctness work, ComplexF32 only\nHDF5.jl the disk tier, save and load\nDiskArrays.jl reading a Funicular file from generic chunked-array code\nLinearMaps.jl the operator adapter, both directions\n\nThe CPU reference backend needs nothing loaded at all. It runs the whole logic layer on plain arrays, and the test suite exercises it everywhere.","title":"Installing it"},{"category":"section","location":"#A-first-look","page":"Home","text":"using CUDA, Funicular, HDF5, LinearAlgebra\n\nbackend = Funicular.cuda_backend()\nplan = ResidencyPlan(backend = backend,\n                     device_budget = 0.7 * CUDA.total_memory(),\n                     host_budget = 200 * 2^30,\n                     scratch_dir = \"/scratch/$(ENV[\"USER\"])/funicular\")\n\nΩ = GhostPanels(ComplexF64, N, k; plan = plan, seed = 0x5EED)\nY = PanelMatrix{ComplexF64}(undef, N, k; plan = plan)\n\npanelmul!(Y, G, Ω)      # stream Ω up, apply G, stream Y down\nR = cholqr2!(Y)         # orthonormalize in place, Y becomes Q\nS = project(Y, G)       # the k × k projected operator Q' G Q\n\nThe demo is a complete runnable version of this, with a known spectrum that it checks itself against.","title":"A first look"},{"category":"section","location":"#What-it-is-not","page":"Home","text":"A PanelMatrix is not an AbstractMatrix and has no getindex. A scalar read of a cold panel costs a panel-sized disk read for sixteen bytes, and cheap-looking syntax should not hide a cost like that. Use Matrix(pm) when the whole matrix fits in host memory, foreachpanel to get one panel at a time on the device, and the operations on this page for everything else.\n\nFunicular is also not a chunked-array standard, not a linear operator standard, and not a solver library. It stores, moves, and multiplies panels. The randomized algorithms that use it live elsewhere.","title":"What it is not"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"When to use this, for deciding whether the package fits your problem, and the arithmetic behind why it can help.\nResidency, for panel width, the three tiers, and how to size the budgets.\nOperators, for what G has to provide.\nThe demo, for a complete program.\nPorting a randomized method, for turning an existing solver into sweeps.\nReference, for the exported names.","title":"Where to go next"},{"category":"section","location":"demo/#Mini-RSVD-on-a-GPU-with-a-disk-tier","page":"Demo","text":"This is a complete, runnable program. It computes the top singular values of a matrix-free operator whose tall-skinny basis does not fit in the host memory it is given, so panels cycle through the disk tier while the GPU works, and it checks the answer against a spectrum it knows in advance.\n\nRun it with a CUDA environment on the path:\n\njulia --project=test/cuda --startup-file=no docs/demo/minirsvd.jl\n\nAlmost all of it is the public API. The only things reached through Funicular. are the traits, the counters at the end that show the disk tier was used, and free!.\n\nIt needs HDF5 loaded for the disk tier, and it writes about three gigabytes under mktempdir(). Point scratch_dir at the fast scratch filesystem a real run would use rather than at a small /tmp.\n\nusing CUDA\nusing Funicular\nusing HDF5\nusing LinearAlgebra\nusing Random","title":"Mini RSVD on a GPU with a disk tier"},{"category":"section","location":"demo/#Sizes","page":"Demo","text":"N is the length of one vector the operator acts on. A real problem puts it in the tens of millions rather than at the million used here. Nothing about the program changes with it, only how long it takes and how much has to spill. K is how many columns the randomized method keeps.\n\nconst T = ComplexF64\nconst N = 1 << 20\nconst K = 64\nconst RANK = 8","title":"Sizes"},{"category":"section","location":"demo/#The-operator","page":"Demo","text":"A stand-in for an expensive matrix-free operator: Hermitian, applied without ever forming a matrix, with a spectrum chosen so the answer is known. U is orthonormal and N × RANK, so G = U diag(σ) U' + ε I has singular values σ .+ ε at the top and ε everywhere below.\n\nFunicular needs four methods from an operator, plus three traits that are optional. panel_capable says that mul! takes a whole panel rather than one column, and workspace_bytes is the device memory the operator holds for itself, which the plan then holds back from the buffer pool. Note that an operator that understates workspace_bytes will cause a run to die of an out-of-memory error halfway through.\n\nstruct LowRankPlusNoise{M,V,C}\n    U::M\n    σ::V\n    ε::Float64\n    work::C\nend\n\nBase.size(G::LowRankPlusNoise) = (size(G.U, 1), size(G.U, 1))\nBase.size(G::LowRankPlusNoise, d::Integer) = d <= 2 ? size(G.U, 1) : 1\nBase.eltype(G::LowRankPlusNoise) = eltype(G.U)\nBase.adjoint(G::LowRankPlusNoise) = G\n\nFunicular.panel_capable(::LowRankPlusNoise) = true\nFunicular.ishermitian_op(::LowRankPlusNoise) = true\nFunicular.workspace_bytes(G::LowRankPlusNoise) = sizeof(G.U) + sizeof(G.work) + sizeof(G.σ)\n\nfunction LinearAlgebra.mul!(Y::AbstractMatrix, G::LowRankPlusNoise, X::AbstractMatrix)\n    C = view(G.work, :, axes(X, 2))\n    mul!(C, G.U', X)\n    C .*= G.σ\n    mul!(Y, G.U, C)\n    Y .+= G.ε .* X\n    Y\nend\n\nfunction LinearAlgebra.mul!(y::AbstractVector, G::LowRankPlusNoise, x::AbstractVector)\n    c = view(G.work, :, 1)\n    mul!(c, G.U', x)\n    c .*= G.σ\n    mul!(y, G.U, c)\n    y .+= G.ε .* x\n    y\nend","title":"The operator"},{"category":"section","location":"demo/#Building-it","page":"Demo","text":"The panel width is fixed here so the numbers below are easy to follow. Left to itself the plan picks the widest panel its device budget allows, aiming at one to four gigabytes per panel.\n\nconst W = 8\n\nRandom.seed!(0xB0A7)\nbasis = CuArray(qr(randn(T, N, RANK)).Q * Matrix{T}(I, N, RANK))\nspectrum = 2.0 .^ -(0:(RANK - 1))\nnoise = 1e-6\nG = LowRankPlusNoise(basis, CuArray(T.(spectrum)), noise, similar(basis, RANK, W))\n\ncheck_operator applies G and its adjoint to a few random probes and complains if the two disagree, if the shapes are wrong, or if either trait is not true. Run it once when you write an operator, not in the inner loop.\n\ncheck_operator(G; backend=Funicular.cuda_backend())","title":"Building it"},{"category":"section","location":"demo/#The-plan","page":"Demo","text":"device_budget is what Funicular may allocate on the GPU: two staging buffers per matrix in a sweep plus the operator's workspace. host_budget caps the page-locked host tier. It is deliberately smaller here than the three N × K matrices this program holds, so it forces panels out to the disk tier rather than letting the whole run sit in memory.\n\nbackend = Funicular.cuda_backend()\nplan = ResidencyPlan(backend=backend,\n                     device_budget=2 * 2^30,\n                     host_budget=2 * 2^30,\n                     workspace_bytes=Funicular.workspace_bytes(G),\n                     scratch_dir=mktempdir(),\n                     panel_width=W)\n\nprintln(\"one panel is \", Base.format_bytes(N * W * sizeof(T)),\n        \"; one matrix is \", Base.format_bytes(N * K * sizeof(T)))","title":"The plan"},{"category":"section","location":"demo/#The-random-test-matrix","page":"Demo","text":"Ω is the Gaussian a randomized method starts from. It is never stored: each column is regenerated from the seed and the column index when a sweep asks for the panel holding it, and dropped when the host tier needs the room. At the sizes this package is for, that saves hundreds of gigabytes of scratch space.\n\nΩ = GhostPanels(T, N, K; plan=plan, seed=0x5EED)","title":"The random test matrix"},{"category":"section","location":"demo/#Subspace-iteration","page":"Demo","text":"The randomized range finder below is three operator applications with an orthonormalization after each. Orthonormalizing between every application rather than after several of them keeps the conditioning of Y inside what CholeskyQR2 can hold.\n\nfunction rangefinder(G, Ω, plan; iterations=2)\n    Y = PanelMatrix{eltype(Ω)}(undef, size(Ω)...; plan=plan)\n    Z = PanelMatrix{eltype(Ω)}(undef, size(Ω)...; plan=plan)\n    panelmul!(Y, G, Ω)\n    cholqr2!(Y)\n    for _ in 1:iterations\n        panelmul!(Z, G, Y)\n        cholqr2!(Z)\n        Y, Z = Z, Y\n    end\n    Y, Z\nend\n\nY, Z = rangefinder(G, Ω, plan)\n\nproject forms the K × K operator Y' G Y without ever storing G Y. It is the expensive one: p (p + 1) panel uploads for p panels, against two sweeps for panelmul! followed by gram. Use it when you cannot afford the second N × K matrix, and panelmul! plus gram when you can.\n\nS = project(Y, G)","title":"Subspace iteration"},{"category":"section","location":"demo/#The-answer","page":"Demo","text":"S is K × K and dense, so the small eigenproblem at the end of every randomized method is a plain LAPACK call on the host.\n\ncomputed = svdvals(S)[1:RANK]\nexpected = spectrum .+ noise\n\nprintln(\"\\n  computed        expected      relative error\")\nfor (c, e) in zip(computed, expected)\n    println(rpad(round(c, sigdigits=8), 16), rpad(round(e, sigdigits=8), 16),\n            round(abs(c - e) / e, sigdigits=3))\nend\n\n@assert isapprox(computed, expected; rtol=1e-6)\n@assert opnorm(gram(Y) - I) < 100 * N * K * eps(real(T))","title":"The answer"},{"category":"section","location":"demo/#What-the-tiers-did","page":"Demo","text":"Panels moved between host and disk because host_budget could not hold three matrices at once. If both counters are zero, the run stayed in host memory and the disk tier was never exercised. Raise K or lower host_budget if you want to see it work.\n\nprintln(\"\\npanel reads from disk:  \", Funicular.disk_reads(Y) + Funicular.disk_reads(Z))\nprintln(\"panel writes to disk:   \", Funicular.disk_writes(Y) + Funicular.disk_writes(Z))","title":"What the tiers did"},{"category":"section","location":"demo/#Keeping-the-basis","page":"Demo","text":"save writes a PanelMatrix as one HDF5 file with a chunk per panel, and load opens it with the panels still on disk, streaming up as they are asked for. That file is the matrix's cold tier rather than a copy of it, so an operation that modifies a panel writes it back.\n\npath = joinpath(mktempdir(), \"basis.h5\")\nsave(Y, path)\nprintln(\"\\nsaved \", Base.format_bytes(filesize(path)), \" to \", path)\n\nreloaded = load(PanelMatrix, path; plan=plan, readonly=true)\n@assert opnorm(gram(reloaded) - I) < 100 * N * K * eps(real(T))\nprintln(\"reloaded and still orthonormal\")\n\nFunicular.free!(reloaded)\nFunicular.free!(Y)\nFunicular.free!(Z)","title":"Keeping the basis"},{"category":"section","location":"demo/#Without-a-GPU","page":"Demo","text":"The same program runs on the reference backend: drop using CUDA, replace the backend line with backend = CPUBackend(), and build basis and the operator's spectrum and workspace as ordinary Arrays. The vendor API is there so that nothing else has to change. It will not be fast, since the reference backend's device is host memory and its queues are tasks, but the logic it runs is the same as on the GPU.","title":"Without a GPU"}]
}
